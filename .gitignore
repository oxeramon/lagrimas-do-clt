# backups baixados pelo app (Ajustes > Backup)
lagrimas-do-clt-*.csv
quitacao-*.csv

# configuração local do Claude Code (a de projeto, .claude/settings.json, é versionada)
.claude/settings.local.json

# lixo de sistema operacional
Thumbs.db
desktop.ini
.DS_Store

# editores
.vscode/
.idea/

# prévia local gerada para inspeção visual, nunca publicada
preview.html

# comparativos de proposta visual, gerados localmente
preview-A.html
preview-B.html
preview-C.html
preview-D.html

# planilhas de fatura preenchidas, não são código
fatura-*.csv

# baseline privado da auditoria: a lista dos dados proibidos NUNCA vai para o
# Git, porque publicá-la é publicar os dados. Ela mora fora do repositório e o
# caminho vem por SANITIZATION_BASELINE_PATH. Estes padrões são só uma rede.
baseline-privado*.json
*baseline*.json
dados-proibidos*
*.dump
*.bak

# artefato público gerado por ferramentas/artefato.mjs; o Pages constrói o dele
_site/
